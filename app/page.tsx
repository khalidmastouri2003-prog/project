export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Zid-Sales</h1>
      <p>
        This is a placeholder home page. The original application source
        (pages, components, and business logic described in the project
        README) was not present in this repository&apos;s git history — only
        configuration files had been committed. This page exists so the site
        builds and deploys; replace it with the real application source.
      </p>
    </main>
  )
}
